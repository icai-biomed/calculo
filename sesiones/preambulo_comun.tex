% ══════════════════════════════════════════════════════════════════════════════
% Preámbulo común a todas las sesiones.
%
% Cada Sesion<NN>-Presentacion.qmd debe incluir este archivo Y definir su
% propio \SesionTitulo, por ejemplo en el YAML de la sesión:
%
%   format:
%     beamer:
%       include-in-header:
%         - _preambulo_comun.tex
%         - text: |
%             \newcommand{\SesionTitulo}{Sesión 01 \\ El método de Newton}
%
% El orden de esas dos entradas no importa: \AtBeginDocument no se evalúa
% hasta \begin{document}, momento en el que TODO el preámbulo (ambas
% entradas, en cualquier orden) ya se ha leído. Si una sesión olvida definir
% \SesionTitulo, \ifdefined usa un texto de aviso en su lugar en vez de dar
% un error de "Undefined control sequence".
% ══════════════════════════════════════════════════════════════════════════════

% ── Extra packages (not loaded automatically by Quarto/pandoc) ────────────────────
\usepackage{amsfonts,amssymb}
\usepackage{tikz}
\usepackage{listings}
\usepackage{cancel}
\usepackage{epstopdf}
\usepackage{float}
\usepackage{mathpazo}
\usepackage{multimedia}
\usepackage{ragged2e}
\usepackage[normalem]{ulem}

% ── Setup ───────────────────────────────────────────────────────
\sloppy
\epstopdfsetup{outdir=./}
\graphicspath{{./fig/}}

% ── Title-page metadata ────────────────────────────────────────────
% Uses \AtBeginDocument so these run after pandoc has set its own placeholders,
% ensuring the correct values appear on the title slide. \SesionTitulo is
% supplied per-session (see header comment above); \ifdefined falls back to a
% visible warning instead of erroring if a session forgets to set it.
\AtBeginDocument{%
  \title{\ifdefined\SesionTitulo\SesionTitulo\else Sesión SIN TÍTULO (falta \string\SesionTitulo)\fi}%
  \subtitle{\small{\it Cálculo - Grado en Ingeniería Biomédica}}%
  \author[Dpto. Matemática Aplicada - ETSI - ICAI]{%
    Dpto. de Matemática Aplicada
    {\small Escuela Técnica Superior de Ingeniería-ICAI}
    Universidad Pontificia Comillas}%
  \date{Curso 2026-27}%
}

% ── Logo placed above the title-page content ───────────────────────────────────
\addtobeamertemplate{title page}{%
  \begin{center}
    \vspace{0.5cm}
    \includegraphics[width=3cm]{../fig/logoIcaiHorizontal.png}
  \end{center}%
}{}

% ── Section title page (chapter-style, centered) ──────────────────────────────────
% Fires on every \section{} call. plain removes header/footer; noframenumbering
% keeps the slide count clean. The title is centered using the theme's own color.
% \AtBeginSection[]{%
%   \begin{frame}[plain,noframenumbering]
%     \centering
%     \vfill
%     {\usebeamerfont{section title}\usebeamercolor[fg]{structure}\insertsection\par}
%     \vfill
%   \end{frame}%
% }

% ── Hyperlink styling (blue + underlined, like a normal web link) ───────────
% hyperref is loaded here (ahead of the template's own load) so \href can be
% redefined at top level. colorlinks is set via \AtBeginDocument because the
% template's own \hypersetup{hidelinks,...} call runs after this file and
% would otherwise override it.
\usepackage{hyperref}
\let\originalhref\href
\renewcommand{\href}[2]{\originalhref{#1}{\uline{#2}}}
\AtBeginDocument{%
  \hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue, filecolor=blue}%
}

% ── Custom theorem-like environments ──────────────────────────────────────
\newtheorem{Def}{Definición}
\newtheorem{Propis}{Propiedades}
\newtheorem{Propi}{Propiedad}
\newtheorem{Ob}{Observación}
\newtheorem{Obs}{Observaciones}
\newtheorem{Ex}{Ejemplo}
\newtheorem{Ej}{Ejercicio}
\newtheorem{Exs}{Ejemplos}
\newtheorem{Teo}{Teorema}
\newtheorem{Pro}{Proposición}
\newtheorem{Cor}{Corolario}
\newtheorem{Proposition}{Proposition}
\newtheorem{Prueba}{Demostración}
\theoremstyle{example}
\newtheorem{Hoja}{Hoja 2A}

% ── Custom commands ───────────────────────────────────────────
\providecommand{\abs}[1]{\lvert#1\rvert}
