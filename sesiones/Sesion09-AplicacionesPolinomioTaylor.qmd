---
title: "Sesión 09. Aplicaciones de los polinomios de Taylor."
lang: es
format:
  beamer:
    theme: Madrid
    fonttheme: default
    fontsize: 10pt
    toc: false          # handled manually below; pandoc's toc:true produces a stray }
    section-titles: false   # \AtBeginSection in preamble.txt creates the section slide
    slide-level: 2      # without this, top-level (#) headings become frames directly
                         # instead of \section commands, so \tableofcontents has nothing
                         # to list and \AtBeginSection never fires
    include-in-header:
      - preambulo_comun.tex
      - text: |
          \newcommand{\SesionTitulo}{Sesión 09 \\ Aplicaciones de los polinomios de Taylor.}
  html: default
  docx: default
---

---

**Sesión 09. Aplicaciones de los polinomios de Taylor.**

![](./portadasSesiones/09-TarabillaComun.JPG){width="80%"}

Tarabilla europea, Villaviciosa, Asturias.

+ [Notebook de la sesión en Colab](https://colab.research.google.com/drive/1ukwLVqFnomVJNxsBhNZAR-UhmgpRi6RN?usp=sharing)


---

```{=latex}
  
  \frametitle{Tabla de contenidos}
  \tableofcontents

```


# Introducción. 

Ahora que sabemos qué son los polinomios de Taylor y hemos aprendido a calcularlos, podemos empezar a utilizarlos como herramienta en varios tipos de problemas. En esta sesión vamos a ver cómo nos pueden ayudar a resolver indeterminaciones y en el cálculo de valores aproximados. 

$\quad$

Al discutir esta segunda aplicación vamos a profundizar más en el resto del polinomio de Taylor, utilizando la *fórmula del error de Lagrange*. Esta fórmula nos va a permitir estimar de qué orden de magnitud (décimas, milésimas, etc.) es el error que cometemos al aproximar una función por el valor de su polinomio de Taylor. La fórmula de Lagrange permite entender el comportamiento y el tamaño del resto al describirlo como una combinación de factores cuyos comportamientos son más sencillos de analizar por separado.



# Límites mediante polinomios de Taylor {.fragile}

\small

::: {.callout-note  icon=false}

## Usando la parte principal de una función.

El problema que queremos plantear se puede describir así: tratamos de resolver una indeterminación de la forma 0/0 con esta estructura de **factores** en el numerador y denominador:
$$L = \lim_{x\to x_0}\dfrac{f_1(x) f_2(x)\cdots f_p(x)}{g_1(x) g_2(x)\cdots g_q(x)}$$
Para resolverlo obtendremos las partes principales $PP(f_i(x))$ y $PP(g_k(x))$ de cada uno de los factores del numerador y denominador. Entonces el límite se conserva al cambiar cada factor por su parte principal:
$$L = \lim_{x\to x_0}\dfrac{PP(f_1(x)) PP(f_2(x))\cdots PP(f_p(x))}{PP(g_1(x)) PP(g_2(x))\cdots PP(g_q(x))}$$
:::

::: {.callout-important  appearance="minimal"}

**Advertencias.** Es esencial trabajar con **factores**. En particular, muchos errores ocurren al cambiar los sumandos de una suma por sus partes principales (sin tener en cuenta la cancelación de la que hablamos en la sesión anterior).  Además, si algún factor $f_i(x)$ o $g_k(x)$ no vale 0 en $x_0$ (no es infinitésimo) su parte principal es ese valor y basta con sustituirlo.

:::


\normalsize

---

\small

::: {.callout-note  icon="false"}

## Limitaciones del método. Regla de L'Hôpital.

El método es muy útil para límites en $x_0 = 0$. Para otros puntos las cosas se pueden complicar y en ese contexto es bueno recordar la Regla de L'Hôpital. Si $f(x)$ y $g(x)$ son infinitésimos en $x_0$, sus polinomios de Taylor no tienen el término constante. Así que si las derivadas primeras no se anulan en $x_0$:
$$
\lim_{x\to x_0}\dfrac{f(x)}{g(x)} = 
\lim_{x\to x_0}\dfrac{0 + f'(x_0)(x - x_0) + O(x-x_0)}{0 + g'(x_0)(x - x_0) + O(x-x_0)} = \dfrac{f'(x_0)}{g'(x_0)}
$$
Si las derivadas se anulan volvemos a repetir el proceso. Eso es, en esencia, la Regla de L'Hôpital.
:::

::: {.callout-tip  appearance="minimal"}

\textbf{\textcolor{blue}{Ejercicio 09A01.}} Usar el método que hemos descrito (o L'Hôpital si conviene) para calcular los siguientes límites:

$(a) \quad \displaystyle\lim_{x\to 0}\dfrac{x - \sin(x)}{x^3}$\qquad
$(b) \quad \displaystyle\lim_{x\to 0}\dfrac{\arcsin(x^2) \cos(x) \tan(\sqrt[3]{x}) \log(1+ x^{2/3})}{x^3 + \sin(x^5)}$


$(c) \quad \displaystyle\lim_{x\to 0}\dfrac{e^{2x} - \cos{2x}}{e^{5x} - \cos{3x}}$. \qquad 
$(d) \quad \displaystyle\lim_{x\to 0}\dfrac{2\cos{x} + x^2 - 2}{x^4}$.


$(e) \quad \displaystyle\lim_{x\to 0}\dfrac{1 - \sqrt{1 + x^4}\cos(2x^2)}{x\, (\tan x -\sinh x)}$ \qquad 
$(f)\quad \displaystyle\color{red}{\lim_{ x\to 2}}\color{black}\dfrac{x^3 - 3x^2 + 4}{x^3 - x^2 -8x + 12}$

:::

\normalsize

---

\small

::: {.callout-note  icon="false"}

## Método de cálculo de la parte principal en un caso particular importante 

Sea $f(x)$ un **infinitésimo en $0$** y sea 
$$PP_0(f(x)) = A x^p$$
Si $g(x)$ es un infinitésimo en $x_0$, entonces:
$$PP_{x_0}(f(g(x))) = A\cdot PP_{x_0}((g(x))^p)$$
Por ejemplo, $PP_0(1 - \cos x) = \frac{x^2}{2}$, luego $A = 1/2, p = 2$ en este caso. Con eso:
$$PP(1 - \cos{(\sin x)}) = \dfrac{PP\left(\sin^2 x\right)}{2} = \dfrac{x^2}{2}$$


:::


::: {.callout-tip  appearance="minimal"}

\textbf{\textcolor{firebrick}{Ejercicio 09B01.}} Calcula la parte principal de $e^{\sin^2 x} - 1$.

\textbf{\textcolor{firebrick}{Ejercicio 09B02.}} Comprueba que si $f(x)$ es un infinitésimo en $x_0$ entonces $PP_{x_0}(\log(1 + f(x))) = PP_{x_0}(f(x))$.

\textbf{\textcolor{firebrick}{Ejercicios Hoja 2B GITI 25-26.}} 22 y 24.

:::

# Fórmula de Lagrange para el resto del polinomio de Taylor

\small

::: {.callout-note  icon="false"}

## Descripción de la estructura del resto

Si $f$ es $n + 1$ veces derivable en un entorno de $x_0$ entonces el *resto* $R_n$ del polinomio de Taylor de orden $n$ para $f$ en $x_0$ viene dado por:
$$
R_n(x) = \dfrac{f^{(n + 1)}(c)}{(n+1)!}(x - x_0)^{n + 1}
$$
con $c$ un valor (en general desconocido) situado entre $x$ y $x_0$. 

:::

\small

**Observaciones:** 

+ La fórmula dice que $R_n(x)$ es casi idéntico al término de orden $n + 1$ del polinomio de Taylor, salvo porque la derivada se calcula en $c$ y no en $x_0$.
+ Aunque $c$ es normalmente desconocido, al saber en qué intervalo se encuentra a menudo podemos fácilmente **acotar** el valor (absoluto) de la derivada $n + 1$ en ese intervalo y así acotar el error al aproximar $f(x)$ por el valor del polinomio de Taylor $P_n(x)$.

\normalsize

---

::: {.callout-tip  appearance="minimal"}

\small

\textbf{\textcolor{blue}{Ejercicio 09A02.}} Calcular de forma aproximada $\sqrt{1.02}$ utilizando el polinomio de Maclaurin de orden 2 de $f(x) = \sqrt{1 + x}$. Usar la fórmula de Lagrange para estimar el error cometido.

$\quad$

\textbf{\textcolor{darkgreen}{Ejercicio 09C01.}} Usa el notebook para revisar este resultado. Supongamos que quieres calcular ese valor con nueve cifras decimales de precisión, de manera que si las muestras en la pantalla de una calculadora todas sean correctas. Busca, por prueba y error, qué orden del polinomio debes usar.

$\quad$

\textbf{\textcolor{blue}{Ejercicio 09A03.}} Calcula $e$ con un error menor que $0.0001$. 

$\quad$

\textbf{\textcolor{darkgreen}{Ejercicio 09C02.}} Usa el notebook para confirmar que tu resultado del anterior ejercicio es correcto.

$\quad$

\textbf{\textcolor{firebrick}{Ejercicios Hoja 2B GITI 25-26.}} 18 y 19.

\normalsize

:::
